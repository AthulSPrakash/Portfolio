export default function Nav(){
    return(
        <nav className="nav">
            <p className="logo">Portfolio</p>
            <a className="switch-page" href="#" aria-label="about page">about</a>
        </nav>
    )
}